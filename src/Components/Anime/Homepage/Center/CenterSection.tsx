import React from 'react';
import { AnimeListType } from '../../../../Types/AnimeTypes';

type Props = {
	AnimeList: AnimeListType;
};

const CenterSection = ({ AnimeList }: Props) => {
	return <div className="CentralSection"></div>;
};

export default CenterSection;
