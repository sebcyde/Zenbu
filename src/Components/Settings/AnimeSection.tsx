import React from 'react';

type Props = {};

const AnimeSection = (props: Props) => {
	return (
		<div className="AnimeSection">
			<h2>Anime Section</h2>
		</div>
	);
};

export default AnimeSection;
