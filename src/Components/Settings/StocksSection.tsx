import React from 'react';

type Props = {};

const StocksSection = (props: Props) => {
	return (
		<div className="StocksSection">
			<h2>Stocks Section</h2>
		</div>
	);
};

export default StocksSection;
